{
  "name": "client",
  "version": "1.0.0",
  "description": "Client side code for limerick generator",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "concurrently \"watchify ./assets/js/index.js -o bundle.js\" \"python -m http.server\"",
    "test": "jest --watch --silent",
    "coverage": "jest --coverage --silent"
  },
  "author": "Elwin Carlos",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.0.0",
    "jest": "^26.6.3",
    "watchify": "^4.0.0"
  }
}
